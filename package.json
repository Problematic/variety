{
  "name": "variety",
  "version": "0.1.1",
  "description": "A tiny JavaScript library for working with random values",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest --watch",
    "prepare": "yarn test && yarn build"
  },
  "author": "Derek Stobbe",
  "license": "MIT",
  "private": false,
  "files": [
    "dist/**/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "prettier": "^1.16.4",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  }
}
